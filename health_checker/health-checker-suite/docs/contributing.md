# Contributing

1. **Keep it Generic**: The `common/` package must NEVER contain Python-specific or React-specific rules.
2. **Type Hints**: All functions must use static type hints.
3. **Tests**: All new utilities must include tests in `tests/`.
4. **SOLID Principles**: Adhere to clean architecture and single-responsibility principles.

# Coding Standards

- **Formatting**: Adhere to standard Python formatting (PEP 8).
- **Docstrings**: Google style.
- **Pydantic**: Use V2 models.
- **Imports**: Avoid circular imports. Rely on `__init__.py` for exporting public symbols.
